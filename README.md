### Java Rush M4 CRUD SQLAlchemy operations by FastAPI

```
git fetch origin
git reset --hard origin/main
```

1. `git fetch origin` — забирает все коммиты с удалённого репозитория, не трогая локальные файлы.
2. `git reset --hard origin/main` — заменить все локальные коммиты на удалённые.
